'use strict';

var _assert = require('assert');

var _assert2 = _interopRequireDefault(_assert);

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _src = require('../src');

var _src2 = _interopRequireDefault(_src);

var _utils = require('../src/utils');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var readFile = function readFile(f) {
  return _fs2.default.readFileSync(f).toString();
};
var getFileSize = function getFileSize(f) {
  return _fs2.default.statSync(f).size;
};

describe('es2015_demo', function () {

  it('复制本地文件成功', function (done) {

    var source = __filename;
    var target = (0, _utils.randomFilename)();
    var onProgress = false;

    (0, _src2.default)(source, target, function (size, total) {

      onProgress = true;
      _assert2.default.equal(size, total);
      _assert2.default.equal(total, getFileSize(source));
    }).then(function (filename) {

      _assert2.default.equal(onProgress, true);
      _assert2.default.equal(target, filename);
      _assert2.default.equal(readFile(source), readFile(target));

      done();
    }).catch(function (err) {
      throw err;
    });
  });
});